brightness.c - prints the average brightness of 
a grayscale image on a scale of 0 to 1.

readaline.c - reads a line of any size from a 
filestream, stores it, and returns the number of bytes
in the line.

simlines.c - prints the similar lines in one
or more files.

